{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "canvas-auth",
  "description": "Minimal IPC capability for Canvas auth WebView — allows data callbacks to Rust",
  "windows": ["canvas-auth"],
  "permissions": [
    "core:default"
  ]
}
