# Preferences

> **Stub.** Written once by `oculus docs` and never overwritten — fill it in
> and it stays. An empty section below means "not stated yet", not "no
> preference".

Standing preferences that hold across every subject. Subject-specific ones
belong in that course's `agents/INSTRUCTIONS.md`.

One bullet per preference, under its heading, written as an instruction —
"lead with the verdict, then the evidence". Wait until it has been said twice;
anything true of me that is not an instruction is a memory, not a line here.

Oculus reads this file into the in-app chat's prompt on every message, so keep
it to preferences that change what an answer looks like.

## Writing

## Working

## Study
